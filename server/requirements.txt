fastapi>=0.110.0
uvicorn[standard]>=0.28.0
websockets>=12.0
pydantic>=2.6.0
httpx>=0.27.0
python-dotenv>=1.0.0
# Chrome renders PDFs in a plugin document with no text in the DOM;
# reading one means parsing the file itself.
pypdf>=4.0.0
